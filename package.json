{
  "name": "update-android-version-gradle-action",
  "version": "1.0.0",
  "description": "This action update the `BundleShortVersionString` and `BundleVersion` properties of the Info.plist file for your iOS projects.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-action": "tsc && ncc build lib/index.js --license licenses.txt"
  },
  "author": "damienaicheh",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.7",
    "@actions/exec": "^1.0.4",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^14.14.41"
  }
}